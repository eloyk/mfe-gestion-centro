import { InscriptionContainer } from '@/feats/management/containers/inscription-container'
import React from 'react'

const Inscription = ({dispatch, router}:any) => <InscriptionContainer dispatch={dispatch} router={router}/>

export default Inscription